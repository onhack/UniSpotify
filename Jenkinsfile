pipeline {
  agent none
  stages {
    stage('Build') {
      steps {
        echo 'Build started'
        sleep(time: 1, unit: 'MINUTES')
      }
    }

    stage('Deploy') {
      steps {
        echo 'Deploy'
      }
    }

  }
}